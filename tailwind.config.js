/** @type {import('tailwindcss').Config} */
module.exports = {
	content: [
		'./**/*.html', // Include all HTML files
		'./blogApp/**/*.html', // Include HTML files in the blogApp directory
	],
	theme: {
		extend: {},
	},
	plugins: [],
};

